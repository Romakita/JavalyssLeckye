/**
 * == lang ==
 * Liste des méthodes et fonctions ajoutés à la bibliothèque standard Javascript.
 *
 * <p class="lang">Le package `lang` est automatiquement chargé par [[Extends]] à l'exception de la classe `extends.lang.countries`.</p>
 **/
Import("extends.lang.class");
Import("extends.lang.date");
Import("extends.lang.number");
Import("extends.lang.observer");
Import("extends.lang.string");
Import("extends.lang.timer");
Import("extends.lang.countries");
Import("extends.lang.clauses");
