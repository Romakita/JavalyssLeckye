/**
 * == Geom ==
 * Le package extends.geom contient des classes géométriques, telles que des points, des rectangles et des matrices de transformation pour effectuer des calcules géométriques.
 **/
Import("extends.geom.point");
Import("extends.geom.rectangle");
//Import('extends.geom.matrix');
//Import("extends.geom.transform");
