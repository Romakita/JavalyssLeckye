/**
 * == Bitmap ==
 * Cette section regroupe les classes permettant de travailler sur les couleurs, pixels et bitmap.
 *
 * <p class="note">Ce package n'est pas chargé automatiquement par `Extends`</p>
 **/
Import("extends.bitmap.color");
