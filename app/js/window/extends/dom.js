/**
 * == DOM ==
 * Cette section regroupe les ajouts de fonctionnalités au DOM Javascript comme le support du drag'n'drop aux elements DOM et la création d'élement de type [[Node]].
 *
 * <p class="note">Le package `DOM` est automatiquement chargé par [[Extends]].</p>
 **/
Import("extends.dom.node");
Import("extends.dom.event");
Import("extends.dom.document");
Import("extends.dom.dragdrop");
Import("extends.dom.effect");
Import("extends.dom.picture");